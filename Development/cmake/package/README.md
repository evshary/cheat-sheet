# package

## Build

```sh
# Enter build
cd build
# Generate Makefile
cmake ..
# Build code
make
# Build package
make package
# Exit build
cd ..
```

## Clean

```sh
# Just remove the files under build folder
rm -rf build/*
```
