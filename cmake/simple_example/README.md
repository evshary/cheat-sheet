# Build
```sh
# Enter build
cd build
# Generate Makefile
cmake ..
# Build code
make
# Show more detail while make
make VERBOSE=1
# Exit build
cd ..
```

# Clean
```sh
# Just remove the files under build folder
rm -rf build/*
```
